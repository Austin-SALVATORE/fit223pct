/**
 * Single source of truth for the product's displayed name.
 *
 * Deliberately a restrained placeholder: the repository is `fit223pct`, but
 * repository naming and product branding are separate concerns, and branding
 * has not been intentionally designed yet. When it is, change it here (and
 * the <title> in index.html, which cannot import TypeScript).
 */
export const PRODUCT_NAME = 'Training'
