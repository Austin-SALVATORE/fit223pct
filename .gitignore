# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Playwright MCP browser session artifacts
.playwright-mcp/

# Exercise asset sources — regenerable from prompt.md via scripts/ (~$0.15/strip);
# runtime AVIFs are committed instead. Exception: the goblet-squat style anchor,
# the irreplaceable ground truth every generation is matched against.
public/assets/exercises/*/reference.png
!public/assets/exercises/goblet-squat/reference.png
generation-run.log

# Third-party reference pack (visual research material for prompt authoring,
# not app content) — 549MB, never shipped, never committed.
public/assets/Free Exercise Pack/
